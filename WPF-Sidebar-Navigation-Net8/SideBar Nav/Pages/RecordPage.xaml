<UserControl x:Class="SideBar_Nav.Pages.RecordPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="600">
    <Grid Margin="20, 0, 0, 0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <!-- Header with title and controls -->
        <DockPanel Grid.Row="0" Margin="0,0,0,20">
            <!-- Title tag -->
            <TextBlock Text="Record" Foreground="#FF454545" FontWeight="SemiBold" FontSize="24" FontFamily="Times New Roman" />
            <!-- Right aligned buttons -->
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right">
                <Button x:Name="btnStart" Content="Start" Margin="5" MinWidth="60" Background="#FF1EA0FF" Foreground="White" />
                <Button x:Name="btnStop" Content="Stop" Margin="5" MinWidth="60" Foreground="White" Background="#FF1EA0FF" />
                <Button x:Name="btnHint" Content="Show Hint" Margin="5" MinWidth="60" Background="#FF1EA0FF" Foreground="White" Click="btnHint_Click" />
            </StackPanel>
        </DockPanel>
        <!-- Conversation section -->
        <StackPanel Grid.Row="1" Margin="0,0,5,0">
            <Border Background="#FF1EA0FF" Padding="6" CornerRadius="4" Margin="0,0,0,5">
                <TextBlock Text="Conversation" Foreground="White" FontWeight="SemiBold" />
            </Border>
            <Border BorderBrush="#e7eaf1" BorderThickness="1" CornerRadius="12" Height="180">
                <RichTextBox x:Name="rtbxConversation" IsReadOnly="True" VerticalScrollBarVisibility="Visible"></RichTextBox>
            </Border>
        </StackPanel>
        <!-- Hint section -->
        <StackPanel x:Name="spHint" Grid.Row="2" Margin="0,0,5,0" Visibility="Hidden">
            <Border Background="#FF1EA0FF" Padding="6" CornerRadius="4" Margin="0,10,0,5">
                <TextBlock Text="Hint" Foreground="White" FontWeight="SemiBold" />
            </Border>
            <Border BorderBrush="#e7eaf1" BorderThickness="1" CornerRadius="12" Height="120">
                <RichTextBox x:Name="rtbxHint" IsReadOnly="True" VerticalScrollBarVisibility="Visible"></RichTextBox>
            </Border>
        </StackPanel>
        <StackPanel Grid.Row="1"/>
    </Grid>
</UserControl>