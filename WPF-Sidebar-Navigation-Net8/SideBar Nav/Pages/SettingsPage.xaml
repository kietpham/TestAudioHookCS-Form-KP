<UserControl x:Class="SideBar_Nav.Pages.SettingsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="600">
    <Grid Margin="20, 0, 0, 0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <!-- Header -->
        <DockPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Settings" Foreground="#FF454545" FontWeight="SemiBold" FontSize="24"/>
        </DockPanel>
        
        <!-- Content -->
        <StackPanel Grid.Row="1" VerticalAlignment="Top" Margin="0,0,5,0">
            <Border Background="#FF1EA0FF" Padding="6" CornerRadius="4" Margin="0,0,0,5">
                <TextBlock Text="Input Device" Foreground="White" FontSize="16" />
            </Border>
            <ComboBox x:Name="InputDeviceCombo" Height="28">
                <ComboBoxItem Content="Speakers (Realtek High Definition Audio)" IsSelected="True"/>
                <ComboBoxItem Content="Headphones" />
                <ComboBoxItem Content="External Mic" />
            </ComboBox>
            <Border Background="#FF1EA0FF" Padding="6" CornerRadius="4" Margin="0,15,0,5">
                <TextBlock Text="Output Device" Foreground="White" FontSize="16" />
            </Border>
            <ComboBox x:Name="OutputDeviceCombo" Height="28">
                <ComboBoxItem Content="Microphone (Realtek High Definition Audio)" IsSelected="True"/>
                <ComboBoxItem Content="Internal Mic" />
                <ComboBoxItem Content="USB Mic" />
            </ComboBox>
        </StackPanel>
    </Grid>
</UserControl>